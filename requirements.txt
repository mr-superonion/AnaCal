# run the following command:
# conda install -c conda-forge --file conda_requirements.txt
# prior to running pip install .

# If your server uses very old compiler, please install # gcc from conda-forge
# as fllows (I do not know how it works on mac or windows):

gcc_linux-64
gxx_linux-64
libxcrypt

fitsio
galsim
numpy
pydantic
setuptools>=38
wheel
pybind11>=2.2
jax
jaxlib
